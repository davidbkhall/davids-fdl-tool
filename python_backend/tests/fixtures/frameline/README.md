Frameline XML fixtures for Phase 4 interop tests.

Optional local fixtures:
- `arri/sample.xml`
- `sony/sample.xml`

You can override the fixture folder with:
- `FDL_FRAMELINE_FIXTURES_DIR=/absolute/path/to/fixtures`

Tests skip gracefully when fixtures or converter packages are unavailable.
